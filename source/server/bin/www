#!/usr/bin/env node
var server = require('../server');
server.listen(process.env.PORT || 3030, function () {
    console.log('Server running in ' + process.env.NODE_ENV + ' environment. Listening on port ' + server.address().port + '...');
});